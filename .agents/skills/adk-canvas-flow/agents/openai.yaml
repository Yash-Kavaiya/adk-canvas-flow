interface:
  display_name: "Adk Canvas Flow"
  short_description: "Repo-specific patterns and workflows for adk-canvas-flow"
  default_prompt: "Use the adk-canvas-flow repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true